{
  "name": "btc-twap",
  "version": "1.0.0",
  "description": "BRTI Multi-Exchange TWAP Drift Dashboard — native iOS app shell",
  "private": true,
  "scripts": {
    "generate:tones": "node scripts/generate-tones.mjs",
    "generate:icon": "powershell -ExecutionPolicy Bypass -File scripts/generate-icon.ps1"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
